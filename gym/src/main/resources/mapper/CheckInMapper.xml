<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gym.mapper.CheckInMapper">
    <select id="getAllCheckIns" resultType="com.gym.entity.CheckIn">
        SELECT * FROM check_in ORDER BY check_in_time DESC
    </select>

    <select id="getCheckInsByMemberAccount" resultType="com.gym.entity.CheckIn">
        SELECT * FROM check_in WHERE member_account = #{memberAccount} ORDER BY check_in_time DESC
    </select>

    <select id="getTodayCheckIn" resultType="com.gym.entity.CheckIn">
        SELECT * FROM check_in
        WHERE member_account = #{memberAccount}
          AND check_in_type = 'GYM'
          AND DATE(check_in_time) = CURDATE() LIMIT 1
    </select>

    <!-- 按预约订单查询课程签到记录（每订单仅一次） -->
    <select id="getClassCheckInByOrder" resultType="com.gym.entity.CheckIn">
        SELECT * FROM check_in
        WHERE class_order_id = #{classOrderId}
          AND check_in_type = 'CLASS'
        LIMIT 1
    </select>

    <insert id="addCheckIn" parameterType="com.gym.entity.CheckIn">
        INSERT INTO check_in (member_account, member_name, class_order_id, check_in_time, check_in_type, remark)
        VALUES (#{memberAccount}, #{memberName}, #{classOrderId}, NOW(), #{checkInType}, #{remark})
    </insert>
</mapper>

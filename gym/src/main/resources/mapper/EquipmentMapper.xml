<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gym.mapper.EquipmentMapper">
    <select id="getAllEquipments" resultType="com.gym.entity.Equipment">
        SELECT * FROM equipment
    </select>

    <select id="getEquipmentById" resultType="com.gym.entity.Equipment">
        SELECT * FROM equipment WHERE equipment_id = #{equipmentId}
    </select>

    <insert id="addEquipment" parameterType="com.gym.entity.Equipment">
        INSERT INTO equipment (equipment_name, equipment_location, equipment_status, equipment_message)
        VALUES (#{equipmentName}, #{equipmentLocation}, #{equipmentStatus}, #{equipmentMessage})
    </insert>

    <update id="updateEquipment" parameterType="com.gym.entity.Equipment">
        UPDATE equipment SET
        equipment_name = #{equipmentName},
        equipment_location = #{equipmentLocation},
        equipment_status = #{equipmentStatus},
        equipment_message = #{equipmentMessage}
        WHERE equipment_id = #{equipmentId}
    </update>

    <delete id="deleteEquipment">
        DELETE FROM equipment WHERE equipment_id = #{equipmentId}
    </delete>
</mapper>

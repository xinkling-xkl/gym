<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gym.mapper.MemberMapper">
    <select id="getAllMembers" resultType="com.gym.entity.Member">
        SELECT * FROM member
    </select>

    <select id="getMemberByAccount" resultType="com.gym.entity.Member">
        SELECT * FROM member WHERE member_account = #{memberAccount}
    </select>

    <insert id="addMember" parameterType="com.gym.entity.Member">
        INSERT INTO member (member_account, member_password, member_name, member_gender, member_age, member_height, member_weight, member_phone, card_time, card_class, card_next_class)
        VALUES (#{memberAccount}, #{memberPassword}, #{memberName}, #{memberGender}, #{memberAge}, #{memberHeight}, #{memberWeight}, #{memberPhone}, #{cardTime}, #{cardClass}, #{cardNextClass})
    </insert>

    <update id="updateMember" parameterType="com.gym.entity.Member">
        UPDATE member SET
        member_password = #{memberPassword},
        member_name = #{memberName},
        member_gender = #{memberGender},
        member_age = #{memberAge},
        member_height = #{memberHeight},
        member_weight = #{memberWeight},
        member_phone = #{memberPhone},
        card_time = #{cardTime},
        card_class = #{cardClass},
        card_next_class = #{cardNextClass}
        WHERE member_account = #{memberAccount}
    </update>

    <delete id="deleteMember">
        DELETE FROM member WHERE member_account = #{memberAccount}
    </delete>

    <update id="deductClass">
        UPDATE member SET card_next_class = card_next_class - 1
        WHERE member_account = #{memberAccount} AND card_next_class > 0
    </update>

    <update id="refundClass">
        UPDATE member SET card_next_class = card_next_class + 1
        WHERE member_account = #{memberAccount}
    </update>
</mapper>

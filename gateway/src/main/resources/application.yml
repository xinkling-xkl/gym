server:
  port: 8086

spring:
  application:
    name: time-server
  config:
    import: "nacos:common.yaml?group=DEFAULT_GROUP"
  datasource:
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai

mybatis:
  type-aliases-package: org.example.test.entity
